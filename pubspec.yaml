name: draggable_gridview
description: 该包帮你给 GridView.builder 添加拖拽功能.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  dotted_border: ^2.0.0+2
  drag_and_drop_gridview: ^1.0.8
  drag_select_grid_view: ^0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
